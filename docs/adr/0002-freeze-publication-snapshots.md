# Freeze publication snapshots

A publication serves an immutable snapshot until its owner explicitly updates it. Historical snapshots remain available to owner-facing feedback views so every annotation keeps the exact content and layout context the client saw; automatic live sync and fuzzy cross-version re-anchoring are rejected.
