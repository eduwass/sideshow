# Isolate external feedback from agents

Client submissions use a separate external-feedback model and never enter Sideshow's trusted comment-to-agent stream automatically. The owner may select submissions and copy a structured prompt, preserving a human trust boundary between an untrusted share-link holder and an agent.
